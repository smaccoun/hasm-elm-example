kind: "Service"
apiVersion: "v1"
metadata:
  name: hasm-elm-api
  labels:
    name: hasm-elm-api
    version: 1.0.0
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: external
  selector:
    name: hasm-elm-api
